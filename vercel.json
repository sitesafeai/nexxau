{
  "buildCommand": "cd app && npm install --legacy-peer-deps && npx prisma generate && npx next build",
  "outputDirectory": "app/.next",
  "installCommand": "cd app && npm install --legacy-peer-deps"
}
